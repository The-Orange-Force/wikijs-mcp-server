# Deferred Items -- Phase 01

1. **src/agent.ts tsc error**: `Cannot find module 'node-fetch'` -- caused by removing `node-fetch` in 01-01 Task 1. Plan 01-02 will delete `src/agent.ts` entirely, resolving this. No action needed now.
2. **src/demo.ts depends on agent.ts**: Will also be deleted in Plan 01-02.
