---
phase: 20-docker-integration-and-default-instructions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - instructions.txt
  - docker-compose.yml
  - .dockerignore
  - .env.example
  - CLAUDE.md
  - README.md
autonomous: true
requirements:
  - DOCK-01
  - DOCK-02

must_haves:
  truths:
    - "A default instructions.txt exists in the repository root with generic placeholder topics"
    - "docker-compose.yml mounts instructions.txt into the container at /app/instructions.txt read-only"
    - "instructions.txt is excluded from the Docker build context via .dockerignore"
    - "MCP_INSTRUCTIONS_PATH is documented in .env.example, CLAUDE.md, and README.md"
  artifacts:
    - path: "instructions.txt"
      provides: "Default MCP instructions template with placeholder topics"
      contains: "[TOPIC"
    - path: "docker-compose.yml"
      provides: "Volume mount for instructions file"
      contains: "instructions.txt:/app/instructions.txt:ro"
    - path: ".dockerignore"
      provides: "Exclusion of instructions.txt from build context"
      contains: "instructions.txt"
    - path: ".env.example"
      provides: "Documented MCP_INSTRUCTIONS_PATH env var"
      contains: "MCP_INSTRUCTIONS_PATH"
    - path: "CLAUDE.md"
      provides: "MCP_INSTRUCTIONS_PATH in env var table"
      contains: "MCP_INSTRUCTIONS_PATH"
    - path: "README.md"
      provides: "MCP_INSTRUCTIONS_PATH in env var table"
      contains: "MCP_INSTRUCTIONS_PATH"
  key_links:
    - from: "docker-compose.yml"
      to: "instructions.txt"
      via: "volume mount"
      pattern: "\\./instructions\\.txt:/app/instructions\\.txt:ro"
    - from: ".dockerignore"
      to: "instructions.txt"
      via: "exclusion entry"
      pattern: "instructions\\.txt"
---

<objective>
Ship a default instructions.txt template and wire Docker Compose to mount it into the container, enabling deployers to customize Claude's MCP instructions without rebuilding the image.

Purpose: Deployers can edit instructions.txt on the host, restart the container, and Claude gets updated behavioral guidance -- no image rebuild required.
Output: instructions.txt at repo root, updated docker-compose.yml with volume mount, updated .dockerignore, and documentation updates across .env.example, CLAUDE.md, README.md.
</objective>

<execution_context>
@/Users/magnetrong/.claude/get-shit-done/workflows/execute-plan.md
@/Users/magnetrong/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-docker-integration-and-default-instructions/20-CONTEXT.md

@docker-compose.yml
@.dockerignore
@.env.example
@CLAUDE.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create instructions.txt and update Docker configuration</name>
  <files>instructions.txt, docker-compose.yml, .dockerignore</files>
  <action>
1. Create `instructions.txt` at the repository root. Content must be:
   - Directive tone ("When the user asks about X, search the wiki first")
   - 2-4 sentences, short and easy to customize
   - Generic template with `[TOPIC]` style placeholders so deployers know what to replace
   - Topics to suggest as placeholders: projects, clients, technologies, internal processes
   - Include a comment line at the top explaining this file is a template to customize

2. Update `docker-compose.yml` to add a `volumes:` section under the `wikijs-mcp-server` service:
   ```yaml
   volumes:
     - ./instructions.txt:/app/instructions.txt:ro
   ```
   Place `volumes:` after `env_file: .env` and before `networks:`. Preserve all existing comments and structure.

3. Update `.dockerignore` to add `instructions.txt` entry. Add it in the "Secrets" block (after `.env.*`) since it's a runtime-mounted file that should not be baked into the image. Add a brief comment explaining why (mounted at runtime, not built into image).
  </action>
  <verify>
    <automated>test -f instructions.txt && grep -q '\[' instructions.txt && grep -q 'instructions.txt:/app/instructions.txt:ro' docker-compose.yml && grep -q 'instructions.txt' .dockerignore && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>instructions.txt exists at repo root with placeholder template content. docker-compose.yml has read-only volume mount for /app/instructions.txt. .dockerignore excludes instructions.txt from build context.</done>
</task>

<task type="auto">
  <name>Task 2: Document MCP_INSTRUCTIONS_PATH in .env.example, CLAUDE.md, and README.md</name>
  <files>.env.example, CLAUDE.md, README.md</files>
  <action>
1. Update `.env.example`: Add a commented-out line for `MCP_INSTRUCTIONS_PATH` at the end of the file, in the optional variables section. Format:
   ```
   # (Optional) Path to custom MCP instructions file
   # Default: /app/instructions.txt (matches docker-compose volume mount)
   # MCP_INSTRUCTIONS_PATH=/app/instructions.txt
   ```

2. Update `CLAUDE.md` env var table (the markdown table under "## Environment Variables"): Add a new row for `MCP_INSTRUCTIONS_PATH`. Place it after `MCP_RESOURCE_DOCS_URL`. Values:
   - Variable: `MCP_INSTRUCTIONS_PATH`
   - Required: No
   - Description: Path to custom MCP instructions file (default: /app/instructions.txt)
   - Example: `/app/instructions.txt`

3. Update `README.md` env var table (under "### Environment Variables" in the Configuration section): Add the same row for `MCP_INSTRUCTIONS_PATH` after `MCP_RESOURCE_DOCS_URL`, matching the same column format as existing rows.
  </action>
  <verify>
    <automated>grep -q 'MCP_INSTRUCTIONS_PATH' .env.example && grep -q 'MCP_INSTRUCTIONS_PATH' CLAUDE.md && grep -q 'MCP_INSTRUCTIONS_PATH' README.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>MCP_INSTRUCTIONS_PATH appears as a commented-out optional variable in .env.example, and as a row in both the CLAUDE.md and README.md environment variable tables.</done>
</task>

</tasks>

<verification>
All artifacts exist and contain expected content:
- `instructions.txt` exists at repo root with `[TOPIC]` placeholders
- `docker-compose.yml` contains volume mount line `./instructions.txt:/app/instructions.txt:ro`
- `.dockerignore` contains `instructions.txt`
- `.env.example` contains commented `MCP_INSTRUCTIONS_PATH`
- `CLAUDE.md` env var table includes `MCP_INSTRUCTIONS_PATH` row
- `README.md` env var table includes `MCP_INSTRUCTIONS_PATH` row
- `npm run build` still passes (no code changes, but sanity check)
- `npm test` still passes (no code changes, but sanity check)
</verification>

<success_criteria>
1. Default instructions.txt exists with generic template content using [TOPIC] placeholders
2. docker-compose.yml mounts instructions.txt into /app/instructions.txt as read-only
3. instructions.txt is excluded from Docker build context
4. MCP_INSTRUCTIONS_PATH documented in all three configuration reference files
5. A deployer can replace instructions.txt on host and restart container to change behavior
</success_criteria>

<output>
After completion, create `.planning/phases/20-docker-integration-and-default-instructions/20-01-SUMMARY.md`
</output>
