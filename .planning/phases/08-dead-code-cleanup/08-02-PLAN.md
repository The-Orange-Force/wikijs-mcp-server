---
phase: 08-dead-code-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/routes/mcp-routes.ts     # Fix JSDoc stale reference
  - src/routes/public-routes.ts  # Fix endpoint map stale reference
  - README.md                    # Fix stale /mcp/events URL
  - QUICK_START.md               # Fix stale /mcp/events URL
  - .planning/ROADMAP.md         # Update success criteria re: scope maps
autonomous: true
requirements: []

must_haves:
  truths:
    - "No production source file contains the string 'mcp/events'"
    - "README.md and QUICK_START.md reference /mcp instead of /mcp/events"
    - "ROADMAP.md Phase 8 success criteria says scope maps are preserved (not deleted)"
    - "Endpoint map in public-routes.ts correctly documents GET /mcp"
    - "JSDoc in mcp-routes.ts correctly documents GET /mcp"
  artifacts:
    - path: "src/routes/mcp-routes.ts"
      provides: "Protected MCP routes with correct JSDoc"
      contains: "POST /mcp and GET /mcp require"
    - path: "src/routes/public-routes.ts"
      provides: "Public routes with correct endpoint map"
      contains: "GET /mcp"
    - path: ".planning/ROADMAP.md"
      provides: "Updated success criteria preserving scope maps"
      contains: "preserved"
  key_links:
    - from: "src/routes/public-routes.ts"
      to: "GET /mcp"
      via: "endpoints object"
      pattern: "GET /mcp.*405"
---

<objective>
Fix all stale GET /mcp/events references in production code and documentation, and update ROADMAP.md success criteria to reflect the scope map preservation decision.

Purpose: Eliminate misleading endpoint references that could confuse clients or developers. The actual endpoint is GET /mcp (per MCP 2025-03-26 spec, decided in Phase 1). Also correct ROADMAP.md which says to delete SCOPE_TOOL_MAP/TOOL_SCOPE_MAP when the user explicitly decided to preserve them for v2 ADVN-01.

Output: All stale references corrected. ROADMAP success criteria aligned with user decision.
</objective>

<execution_context>
@/Users/magnetrong/.claude/get-shit-done/workflows/execute-plan.md
@/Users/magnetrong/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-dead-code-cleanup/08-RESEARCH.md

Key facts from research:
- mcp-routes.ts line 4: JSDoc says "GET /mcp/events" -- should say "GET /mcp"
- public-routes.ts line 46: endpoint map says "GET /mcp/events" -- should say "GET /mcp"
- README.md line 163: "events": "http://localhost:3200/mcp/events" -- update to /mcp
- README.md line 178: description says /mcp/events -- update to /mcp
- QUICK_START.md line 43: "events": "http://localhost:3200/mcp/events" -- update to /mcp
- ROADMAP.md Phase 8 success criteria #3: says "removed" but user decided "preserved"
- .planning/ historical files should NOT be updated (except ROADMAP.md per explicit request)

Exact fixes (from RESEARCH.md):

mcp-routes.ts JSDoc fix:
  Current: "POST /mcp and GET /mcp/events require a valid Bearer token."
  Fixed: "POST /mcp and GET /mcp require a valid Bearer token."

public-routes.ts endpoint map fix:
  Current: "GET /mcp/events": "MCP SSE events (requires Bearer token)"
  Fixed: "GET /mcp": "MCP SSE endpoint -- returns 405 in stateless mode (requires Bearer token)"

ROADMAP.md success criteria fix:
  Current: "3. Unused `SCOPE_TOOL_MAP` and `TOOL_SCOPE_MAP` are removed from `src/scopes.ts`"
  Fixed: "3. `SCOPE_TOOL_MAP` and `TOOL_SCOPE_MAP` are preserved in `src/scopes.ts` for v2 per-tool scope enforcement (ADVN-01)"
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix stale endpoint references in production code</name>
  <files>src/routes/mcp-routes.ts, src/routes/public-routes.ts</files>
  <action>
Fix the two stale GET /mcp/events references in production source files:

1. In `src/routes/mcp-routes.ts` line 4:
   Change: `POST /mcp and GET /mcp/events require a valid Bearer token.`
   To: `POST /mcp and GET /mcp require a valid Bearer token.`

2. In `src/routes/public-routes.ts` line 46 (the endpoints object):
   Change: `"GET /mcp/events": "MCP SSE events (requires Bearer token)",`
   To: `"GET /mcp": "MCP SSE endpoint -- returns 405 in stateless mode (requires Bearer token)",`

When touching these JSDoc/comments, review the surrounding documentation for accuracy and improve clarity if any other statements are stale or misleading. The core change is replacing /mcp/events with /mcp and updating the description.

After the edits, verify:
- `grep -r 'mcp/events' src/` returns empty (no stale refs remain in production code).
- `npx tsc --noEmit` succeeds (no TypeScript breakage from comment/string changes).
  </action>
  <verify>
    <automated>grep -r 'mcp/events' src/ && echo "FAIL: stale refs found" && exit 1 || echo "PASS: no stale refs in src/" && npx tsc --noEmit</automated>
  </verify>
  <done>Zero occurrences of "mcp/events" in any file under src/. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Fix stale references in docs and update ROADMAP success criteria</name>
  <files>README.md, QUICK_START.md, .planning/ROADMAP.md</files>
  <action>
Fix stale endpoint references in user-facing documentation and update the ROADMAP:

1. In `README.md` line 163:
   Change: `"events": "http://localhost:3200/mcp/events",`
   To: `"events": "http://localhost:3200/mcp",`

2. In `README.md` line 178:
   Change: `**\`events: "http://localhost:3200/mcp/events"\`** - URL for Server-Sent Events`
   To: `**\`events: "http://localhost:3200/mcp"\`** - URL for Server-Sent Events (returns 405 in stateless mode)`

3. In `QUICK_START.md` line 43:
   Change: `"events": "http://localhost:3200/mcp/events",`
   To: `"events": "http://localhost:3200/mcp",`

4. In `.planning/ROADMAP.md`, find the Phase 8 success criteria line that says:
   `3. Unused \`SCOPE_TOOL_MAP\` and \`TOOL_SCOPE_MAP\` are removed from \`src/scopes.ts\``
   Replace with:
   `3. \`SCOPE_TOOL_MAP\` and \`TOOL_SCOPE_MAP\` are preserved in \`src/scopes.ts\` for v2 per-tool scope enforcement (ADVN-01)`

IMPORTANT: Do NOT modify any .planning/ files other than ROADMAP.md. Historical planning documents are records, not living docs.

After the edits, verify:
- `grep -r 'mcp/events' README.md QUICK_START.md` returns empty.
- ROADMAP.md Phase 8 success criteria #3 says "preserved" not "removed".
  </action>
  <verify>
    <automated>grep 'mcp/events' README.md QUICK_START.md && echo "FAIL" && exit 1 || echo "PASS: docs clean" && grep 'preserved' .planning/ROADMAP.md | grep -q 'SCOPE_TOOL_MAP' && echo "PASS: ROADMAP updated" || echo "FAIL: ROADMAP not updated"</automated>
  </verify>
  <done>README.md and QUICK_START.md reference /mcp (not /mcp/events). ROADMAP.md Phase 8 success criteria correctly says scope maps are preserved for v2.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. `grep -r 'mcp/events' src/` -- returns empty (production code clean)
2. `grep 'mcp/events' README.md QUICK_START.md` -- returns empty (docs clean)
3. `grep 'preserved.*SCOPE_TOOL_MAP' .planning/ROADMAP.md` -- returns match (ROADMAP updated)
4. `npx tsc --noEmit` -- zero errors
5. `npx vitest run` -- all tests still pass (these are content-only changes)
</verification>

<success_criteria>
- Zero occurrences of "mcp/events" in production source files (src/)
- Zero occurrences of "mcp/events" in README.md and QUICK_START.md
- ROADMAP.md Phase 8 success criteria reflects scope map preservation decision
- All tests pass (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/08-dead-code-cleanup/08-02-SUMMARY.md`
</output>
