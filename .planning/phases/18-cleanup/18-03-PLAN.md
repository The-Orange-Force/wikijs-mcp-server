---
phase: 18-cleanup
plan: 03
type: execute
wave: 2
depends_on: [18-01, 18-02]
files_modified:
  - tests/helpers/build-test-app.ts
  - tests/smoke.test.ts
  - tests/scopes.test.ts
  - tests/observability.test.ts
  - tests/discovery.test.ts
  - tests/oauth-proxy-discovery.test.ts
  - tests/e2e-flow.test.ts
  - README.md
  - CLAUDE.md
autonomous: true
requirements: [CLEN-01, CLEN-02, CLEN-03]

must_haves:
  truths:
    - "npm test passes with zero failures"
    - "npm run build compiles cleanly"
    - "mockWikiJsApi exists in exactly one location (tests/helpers/build-test-app.ts)"
    - "smoke.test.ts imports mockWikiJsApi from shared helper, does not define its own"
    - "scopes.test.ts asserts exactly 1 scope (wikijs:read) and the correct number of surviving tools"
    - "No test file references list_users, create_page, delete_page, or any dead tool name"
    - "discovery.test.ts and oauth-proxy-discovery.test.ts assert 1 scope, not 3"
    - "README.md describes a 3-tool read-only MCP server with no STDIO references"
    - "CLAUDE.md matches the actual codebase (tool count, transport, scopes, project structure)"
  artifacts:
    - path: "tests/helpers/build-test-app.ts"
      provides: "Single shared mockWikiJsApi with only surviving method stubs"
    - path: "tests/scopes.test.ts"
      provides: "Scope tests for wikijs:read only"
    - path: "tests/smoke.test.ts"
      provides: "Smoke tests for surviving tools only"
    - path: "README.md"
      provides: "Accurate documentation for 3-tool read-only server"
    - path: "CLAUDE.md"
      provides: "Accurate agent guidance matching current codebase"
  key_links:
    - from: "tests/smoke.test.ts"
      to: "tests/helpers/build-test-app.ts"
      via: "import mockWikiJsApi"
      pattern: "import.*mockWikiJsApi.*from.*build-test-app"
    - from: "tests/scopes.test.ts"
      to: "src/scopes.ts"
      via: "import SCOPES"
      pattern: "import.*SCOPES.*from.*scopes"
---

<objective>
Update all tests to match the consolidated 3-tool, 1-scope codebase and rewrite documentation.

Purpose: After Plans 01 and 02 remove dead source code and infrastructure, test assertions still reference 17 tools, 3 scopes, and dead tool names. Tests will fail at runtime (string-based tool names are not type-checked). Documentation still describes the old architecture. This plan makes tests green and documentation accurate.

Output: All tests passing, consolidated mock, accurate README and CLAUDE.md.
</objective>

<execution_context>
@/Users/magnetrong/.claude/get-shit-done/workflows/execute-plan.md
@/Users/magnetrong/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-cleanup/18-RESEARCH.md
@.planning/phases/18-cleanup/18-01-SUMMARY.md
@.planning/phases/18-cleanup/18-02-SUMMARY.md
</context>

<interfaces>
<!-- From tests/helpers/build-test-app.ts (after Plan 01 cleanup): -->
<!-- mockWikiJsApi should only have stubs for surviving API methods: -->
<!-- checkConnection, getPageById, getPageContent, getPagesList, searchPages -->

<!-- From src/scopes.ts (after Plan 01 cleanup): -->
<!-- SCOPES = { READ: "wikijs:read" } -->
<!-- SCOPE_TOOL_MAP = { "wikijs:read": ["get_page", "list_pages", "search_pages"] } -->
<!-- SUPPORTED_SCOPES = ["wikijs:read"] -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Consolidate mockWikiJsApi and update all test files</name>
  <files>tests/helpers/build-test-app.ts, tests/smoke.test.ts, tests/scopes.test.ts, tests/observability.test.ts, tests/discovery.test.ts, tests/oauth-proxy-discovery.test.ts, tests/e2e-flow.test.ts</files>
  <action>
**Step 1: Clean the shared mockWikiJsApi in tests/helpers/build-test-app.ts**

Remove dead stubs from the existing `mockWikiJsApi` object. Keep only:
- `checkConnection` -- used by health check
- `getPageById` -- used by get_page
- `getPageContent` -- used by get_page_content (if it survives Phase 15; check mcp-tools.ts)
- `getPagesList` -- used by list_pages
- `searchPages` -- used by search_pages

Remove: `createPage`, `updatePage`, `deletePage`, `getUsersList`, `searchUsers`, `getGroupsList`, `createUser`, `updateUser`, `getAllPagesList`, `searchUnpublishedPages`, `forceDeletePage`, `getPageStatus`, `publishPage`.

**Step 2: Consolidate smoke.test.ts to use shared mock**

In `tests/smoke.test.ts`:
1. Remove the local `mockWikiJsApi` definition (lines 16-53) and the local `makeTestConfig` function (lines 62-77).
2. Add imports from the shared helper: `import { mockWikiJsApi, makeTestConfig, buildTestApp } from "./helpers/build-test-app.js";`
3. Remove the manual Fastify server setup in `beforeAll` (lines 79-116). Replace with `buildTestApp()` usage:
   ```typescript
   let app: FastifyInstance;
   let validToken: string;
   beforeAll(async () => {
     validToken = await createTestToken();
     app = await buildTestApp();
     await app.listen({ port: 0, host: "127.0.0.1" });
     baseUrl = `http://127.0.0.1:${(app.server.address() as any).port}`;
   });
   ```
   Note: smoke.test.ts uses real `fetch()` (not `app.inject()`), so it needs a listening server with a real port. Adapt accordingly.
4. Update tools/list test (line 181): Change description from "all 17 tools" to match surviving count.
5. Update line 197: Change `.toBe(17)` to match surviving tool count.
6. Update `expectedTools` array (lines 210-228): Remove all dead tool names, keep only surviving tools.
7. Rewrite the "tools/call invokes list_users" test (lines 234-261): Change to test a surviving tool like `list_pages`:
   ```typescript
   it("POST /mcp with tools/call invokes list_pages tool with mock", async () => {
     const res = await mcpPost({
       jsonrpc: "2.0", id: 3, method: "tools/call",
       params: { name: "list_pages", arguments: {} },
     });
     expect(res.status).toBe(200);
     const data = await res.json();
     expect(data.result.content).toBeDefined();
     expect(data.result.content[0].type).toBe("text");
     const parsed = JSON.parse(data.result.content[0].text);
     expect(Array.isArray(parsed)).toBe(true);
   });
   ```
8. Update the GET / version assertion (line 274): Change `"2.0.0"` to `"2.3.0"`.

**Step 3: Rewrite scopes.test.ts**

Complete rewrite of `tests/scopes.test.ts` for the single-scope model:
- Assert `SUPPORTED_SCOPES` contains exactly `["wikijs:read"]` (length 1)
- Assert total tool count matches surviving tools (3)
- Assert `SCOPE_TOOL_MAP[SCOPES.READ]` contains exactly the surviving tools
- Assert no duplicate tools
- Assert `TOOL_SCOPE_MAP` reverse lookup covers all tools
- Assert `TOOL_SCOPE_MAP["get_page"]` returns `"wikijs:read"`
- Assert `SCOPES.READ === "wikijs:read"`
- Remove all tests for SCOPES.WRITE and SCOPES.ADMIN
- Remove assertions for write/admin tool names

**Step 4: Update observability.test.ts**

The "Tool observability integration" section (line 509) tests `list_users`. Change to test a surviving tool:
- Replace `list_users` with `list_pages` (also a zero-arg tool, so the test intent is preserved)
- Update the test description and assertions to match

**Step 5: Update discovery.test.ts**

Line 59-68: The "contains scopes_supported with all three scopes" test. Update to:
- Assert `scopes_supported` contains `["wikijs:read"]`
- Assert `scopes_supported` has length 1
- Update test description to "contains scopes_supported with wikijs:read"

**Step 6: Update oauth-proxy-discovery.test.ts**

Lines 100-109: Same pattern as discovery.test.ts. Update to assert 1 scope.

**Step 7: Update e2e-flow.test.ts**

Line 108: The authorize step uses `scope: "wikijs:read wikijs:write"`. Change to `scope: "wikijs:read"`.
Line 124: `expect(scope).toContain("wikijs:write")` -- remove this assertion or change to assert `wikijs:read` only.
Review the rest of e2e-flow.test.ts for any other write/admin scope references.

**After all changes, run `npm test` to verify all tests pass.**
  </action>
  <verify>
    <automated>cd /Users/magnetrong/git/wikijs-mcp-server && npm test 2>&1 | tail -20</automated>
  </verify>
  <done>
- npm test passes with zero failures
- mockWikiJsApi defined only in tests/helpers/build-test-app.ts
- smoke.test.ts imports from shared helper, no local mock
- scopes.test.ts asserts 1 scope and correct tool count
- No test references dead tool names (list_users, create_page, delete_page, etc.)
- discovery and oauth-proxy-discovery tests assert 1 scope
- e2e-flow test uses wikijs:read scope only
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite README.md and update CLAUDE.md</name>
  <files>README.md, CLAUDE.md</files>
  <action>
**README.md -- Full rewrite per user decision.**

Write a new README.md from scratch that accurately describes the current 3-tool read-only MCP server. Structure (Claude's discretion on exact wording):

1. **Title and badges** -- WikiJS MCP Server
2. **Overview** -- Read-only MCP server bridging AI assistants with Wiki.js. Emphasize: search, read, list pages. Mention OAuth 2.1 (Azure AD) authentication.
3. **Features** -- 3 tools (get_page, list_pages, search_pages), OAuth 2.1 resource server, HTTP transport with Fastify
4. **Quick Start** -- Prerequisites (Node 20+), clone, npm install, copy example.env, npm run build, npm start
5. **Configuration** -- Environment variables table (WIKIJS_BASE_URL, WIKIJS_TOKEN, AZURE_TENANT_ID, AZURE_CLIENT_ID, MCP_RESOURCE_URL, PORT)
6. **Available Tools** -- Table with tool name, description, parameters for each of the 3 tools
7. **Authentication** -- Azure AD OAuth 2.1, Bearer token, wikijs:read scope
8. **API Endpoints** -- GET /, GET /health, POST /mcp, discovery endpoints
9. **Docker** -- docker build, docker-compose, Alpine-based image
10. **Development** -- npm run dev, npm test, npm run build
11. **Project Structure** -- Updated tree (no lib/, no STDIO mentions)
12. **License** -- MIT

Remove ALL of the following from the old README:
- STDIO transport sections, Cursor/VS Code STDIO configs
- User Management, Group Management sections
- Write tool examples (create_page, update_page, etc.)
- References to 17 tools, 3 scopes, wikijs:write, wikijs:admin
- `npm run server:stdio` from scripts
- `lib/` from project structure

**CLAUDE.md -- Update per user decision.**

Read the current CLAUDE.md and make targeted updates:
1. Transport line: Remove "and STDIO" -- HTTP only
2. Tools line: Update from "17 registered MCP tools" to the surviving count (3)
3. Project Structure: Remove `lib/` entry and its description
4. Remove `lib/` from structure listing
5. Essential Commands: Remove any STDIO-related commands
6. Security Notes: Update scopes from "3 scopes" to "1 scope: wikijs:read"
7. Current State section: Update milestone to v2.3, update test count, update line count, remove "per-tool scope enforcement" from next steps
8. Architecture Patterns > Adding a New MCP Tool: Update step 6 to reference single scope
9. Update tool names in examples if they reference dead tools

Run `npm run build` as final check.
  </action>
  <verify>
    <automated>cd /Users/magnetrong/git/wikijs-mcp-server && npm run build && ! grep -i "stdio\|mcp_wikijs_stdin\|17 tools\|17 registered\|wikijs:write\|wikijs:admin\|list_users\|create_page\|server:stdio" README.md CLAUDE.md && echo "DOCS OK"</automated>
  </verify>
  <done>
- README.md describes 3-tool read-only server with no STDIO, no write tools, no admin tools
- CLAUDE.md matches actual codebase (tool count, transport, scopes, structure)
- No references to dead tools, dead scopes, or STDIO in either file
- npm run build still passes
  </done>
</task>

</tasks>

<verification>
- `npm test` passes with zero failures
- `npm run build` compiles cleanly
- `grep -r "list_users\|create_page\|delete_page\|wikijs:write\|wikijs:admin" tests/ README.md CLAUDE.md` returns no matches
- `grep -c "mockWikiJsApi" tests/helpers/build-test-app.ts` returns 1+ (shared mock exists)
- `grep -c "mockWikiJsApi" tests/smoke.test.ts` returns only import references, not definitions
- README.md contains "get_page", "list_pages", "search_pages" (surviving tools)
- CLAUDE.md does not contain "lib/" in project structure
</verification>

<success_criteria>
All tests pass with zero failures. Test mock consolidated to single location. All test assertions match the 3-tool, 1-scope architecture. README and CLAUDE.md accurately describe the current codebase with no stale references.
</success_criteria>

<output>
After completion, create `.planning/phases/18-cleanup/18-03-SUMMARY.md`
</output>
