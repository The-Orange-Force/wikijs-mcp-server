---
phase: 08-dead-code-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/auth-errors.ts         # DELETE
  - tests/auth-errors.test.ts  # DELETE
  - src/tools.ts               # DELETE
  - src/schemas.ts             # DELETE
  - src/server.ts              # Remove buildServer export
autonomous: true
requirements: []

must_haves:
  truths:
    - "src/auth-errors.ts no longer exists in the codebase"
    - "tests/auth-errors.test.ts no longer exists in the codebase"
    - "src/tools.ts no longer exists in the codebase"
    - "src/schemas.ts no longer exists in the codebase"
    - "buildServer export no longer exists in src/server.ts"
    - "All remaining tests pass after deletions"
    - "TypeScript compilation succeeds after deletions"
  artifacts:
    - path: "src/server.ts"
      provides: "buildApp factory and start() only -- no buildServer"
      contains: "export function buildApp"
  key_links:
    - from: "src/server.ts"
      to: "src/config.ts"
      via: "config import"
      pattern: "import.*config"
---

<objective>
Delete all orphaned files and dead exports identified by the milestone audit and research sweep.

Purpose: Remove 4 dead files (auth-errors.ts, auth-errors.test.ts, tools.ts, schemas.ts) totaling ~76K+ bytes and the unused buildServer export from server.ts. This leaves only live code in the codebase.

Output: Leaner codebase with no orphaned modules or dead exports. All remaining tests pass.
</objective>

<execution_context>
@/Users/magnetrong/.claude/get-shit-done/workflows/execute-plan.md
@/Users/magnetrong/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-dead-code-cleanup/08-RESEARCH.md

Key facts from research:
- src/auth-errors.ts (183 lines): Zero imports from any production file. Parallel implementation at src/auth/errors.ts is the live path.
- tests/auth-errors.test.ts (149 lines): Only tests the orphaned auth-errors.ts.
- src/tools.ts (~65K bytes): Zero imports anywhere. Legacy pre-SDK tool implementation superseded by src/mcp-tools.ts.
- src/schemas.ts (~10K bytes): Zero imports anywhere. Legacy Zod schemas superseded by inline schemas in src/mcp-tools.ts.
- src/types.ts is NOT dead code -- it is imported by src/api.ts (live WikiJsApi). Do NOT delete types.ts.
- graphql/graphql-request are NOT unused -- src/api.ts imports them. Do NOT remove from package.json.
- buildServer in server.ts line 63-66: Zero consumers. Comment says "legacy export" but nothing uses it.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete orphaned files</name>
  <files>src/auth-errors.ts, tests/auth-errors.test.ts, src/tools.ts, src/schemas.ts</files>
  <action>
Delete these 4 files entirely using `rm`:

1. `rm src/auth-errors.ts` -- 183-line orphaned module, no production consumer. Live path is src/auth/errors.ts.
2. `rm tests/auth-errors.test.ts` -- 149-line test file that only tests the orphaned module.
3. `rm src/tools.ts` -- ~65K bytes legacy pre-SDK tool implementation. Zero imports anywhere.
4. `rm src/schemas.ts` -- ~10K bytes legacy Zod schemas for old tool system. Zero imports anywhere.

IMPORTANT: Do NOT delete src/types.ts. It is imported by src/api.ts (the live WikiJsApi class).
IMPORTANT: Do NOT remove graphql or graphql-request from package.json. src/api.ts uses them.

After deletion, verify no broken imports exist:
- Run `npx tsc --noEmit` to confirm TypeScript compilation succeeds.
- Run `grep -r 'auth-errors' src/ tests/` to confirm no remaining imports of deleted module.
- Run `grep -r "from.*['\"]\.\.?/tools['\"]" src/ tests/` to confirm no remaining imports of deleted tools.ts.
- Run `grep -r "from.*['\"]\.\.?/schemas['\"]" src/ tests/` to confirm no remaining imports of deleted schemas.ts.
  </action>
  <verify>
    <automated>test ! -f src/auth-errors.ts && test ! -f tests/auth-errors.test.ts && test ! -f src/tools.ts && test ! -f src/schemas.ts && npx tsc --noEmit && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>All 4 files deleted. TypeScript compiles cleanly. No broken imports.</done>
</task>

<task type="auto">
  <name>Task 2: Remove buildServer dead export and verify full test suite</name>
  <files>src/server.ts</files>
  <action>
In src/server.ts, remove lines 63-66 (the buildServer export):

```
// Keep legacy export for backward compatibility (Phase 1/2 consumers)
export function buildServer(wikiJsApi: WikiJsApi): FastifyInstance {
  return buildApp(config, wikiJsApi);
}
```

Delete those 4 lines entirely. Do not replace them with anything. The blank line before line 63 (if any) can stay or go -- keep the file clean.

After removal, verify:
- `grep -r 'buildServer' src/ tests/` returns empty (no consumers).
- `npx tsc --noEmit` succeeds.
- `npx vitest run` -- all remaining tests pass. Expect ~93 tests across 8 files (down from 106 tests across 9 files due to auth-errors.test.ts deletion in Task 1).
  </action>
  <verify>
    <automated>npx vitest run && npx tsc --noEmit && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>buildServer export removed from server.ts. All remaining tests pass (~93 tests in 8 files). TypeScript compiles cleanly. grep for buildServer in src/ and tests/ returns empty.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. `test ! -f src/auth-errors.ts` -- file deleted
2. `test ! -f tests/auth-errors.test.ts` -- file deleted
3. `test ! -f src/tools.ts` -- file deleted
4. `test ! -f src/schemas.ts` -- file deleted
5. `grep -r 'buildServer' src/ tests/` -- returns empty
6. `npx tsc --noEmit` -- zero errors
7. `npx vitest run` -- all tests pass (~93 tests in 8 files)
</verification>

<success_criteria>
- 4 orphaned files deleted from the codebase
- buildServer dead export removed from server.ts
- TypeScript compilation succeeds with zero errors
- All remaining tests pass (no regressions from deletions)
</success_criteria>

<output>
After completion, create `.planning/phases/08-dead-code-cleanup/08-01-SUMMARY.md`
</output>
