---
phase: 22-core-gdpr-predicate
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/gdpr.ts
  - src/__tests__/gdpr.test.ts
autonomous: true
requirements:
  - FILT-01
  - FILT-02

must_haves:
  truths:
    - "isBlocked('Clients/AcmeCorp') returns true"
    - "isBlocked('clients/acme') returns true (case-insensitive)"
    - "isBlocked('Clients') returns false (1 segment)"
    - "isBlocked('Clients/Acme/SubPage') returns false (3+ segments)"
    - "isBlocked('/Clients/AcmeCorp/') returns true (slash normalization)"
    - "isBlocked('Clients//AcmeCorp') returns true (double slash normalization)"
    - "isBlocked(null) returns false (defensive null handling)"
    - "isBlocked('') returns false (empty string)"
  artifacts:
    - path: "src/gdpr.ts"
      provides: "GDPR path-blocking predicate"
      exports: ["isBlocked"]
      min_lines: 8
    - path: "src/__tests__/gdpr.test.ts"
      provides: "Full unit test coverage for isBlocked()"
      min_lines: 60
  key_links:
    - from: "src/__tests__/gdpr.test.ts"
      to: "src/gdpr.ts"
      via: "import { isBlocked } from '../gdpr.js'"
      pattern: "import.*isBlocked.*from.*gdpr"
---

<objective>
Implement and test `isBlocked()` -- the GDPR path-blocking predicate that determines whether a Wiki.js page path identifies a protected client page.

Purpose: This is the single source of truth for GDPR path filtering. Phase 23 will import `isBlocked` from this module to enforce filtering in all three MCP tool handlers. The function must be hardened with full edge-case coverage before integration.

Output: `src/gdpr.ts` (production module) and `src/__tests__/gdpr.test.ts` (unit tests)
</objective>

<execution_context>
@/Users/magnetrong/.claude/get-shit-done/workflows/execute-plan.md
@/Users/magnetrong/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-core-gdpr-predicate/22-CONTEXT.md
@.planning/phases/22-core-gdpr-predicate/22-RESEARCH.md

@src/oauth-proxy/__tests__/scope-mapper.test.ts (test pattern to follow)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD isBlocked() predicate -- RED then GREEN</name>
  <files>src/__tests__/gdpr.test.ts, src/gdpr.ts</files>
  <behavior>
    - Blocked paths (exactly 2 segments, first is "clients" case-insensitive):
      - "Clients/AcmeCorp" -> true
      - "clients/acme" -> true
      - "CLIENTS/ACME" -> true
      - "cLiEnTs/SomeCorp" -> true
    - Allowed paths (not blocked):
      - "Clients" -> false (1 segment)
      - "Clients/Acme/SubPage" -> false (3 segments)
      - "Projects/AcmeCorp" -> false (wrong first segment)
      - "home" -> false (root-level page)
    - Normalization variants:
      - "/Clients/AcmeCorp" -> true (leading slash)
      - "Clients/AcmeCorp/" -> true (trailing slash)
      - "Clients//AcmeCorp" -> true (double slashes)
      - "/Clients/AcmeCorp/" -> true (leading + trailing slashes)
    - Null/undefined/empty input:
      - "" -> false
      - null (as any) -> false
      - undefined (as any) -> false
    - Unicode and special characters:
      - "Clients/United" -> true (unicode company name -- use actual Umlaut "Clients/Ünited" per CONTEXT.md)
      - "Clients/Acme Corp" -> true (spaces)
      - "Clients/O'Brien" -> true (apostrophes)
    - Path traversal (literal segments):
      - "../Clients/AcmeCorp" -> false (3 segments with "..")
      - "./Clients/AcmeCorp" -> false (3 segments with ".")
  </behavior>
  <action>
    **RED phase:**
    1. Create `src/__tests__/` directory (does not exist yet).
    2. Create `src/__tests__/gdpr.test.ts` with all test cases from the behavior block above. Follow the `scope-mapper.test.ts` pattern exactly: import `{ describe, it, expect }` from "vitest", import `{ isBlocked }` from `"../gdpr.js"` (`.js` extension required for NodeNext ESM).
    3. Organize tests into describe blocks by scenario category:
       - "blocked paths (exactly 2 segments, first is 'clients')"
       - "allowed paths (not blocked)"
       - "normalization variants"
       - "null, undefined, and empty input"
       - "unicode and special characters"
       - "path traversal (treated as literal segments)"
    4. Run `npx vitest run src/__tests__/gdpr.test.ts` -- tests MUST fail (module not found).
    5. Commit: `test(22-01): add failing tests for isBlocked() GDPR predicate`

    **GREEN phase:**
    6. Create `src/gdpr.ts` with the `isBlocked()` function. Per user decisions:
       - Only `isBlocked()` is exported. `normalizePath()` (if used) is internal -- not exported.
       - "clients" literal hardcoded inside the function, NOT a module-level constant.
       - Guard `if (!path) return false;` before any string ops (handles null/undefined/empty).
       - Core logic: `path.toLowerCase().split("/").filter(Boolean)` then check `segments.length === 2 && segments[0] === "clients"`.
       - Add JSDoc comment describing the predicate behavior and examples.
    7. Run `npx vitest run src/__tests__/gdpr.test.ts` -- ALL tests MUST pass.
    8. Commit: `feat(22-01): implement isBlocked() GDPR path predicate`

    **REFACTOR phase (if needed):**
    9. Review for clarity. The function should be ~5-8 lines of actual logic. If already clean, skip this step.
    10. Run `npx vitest run` (full suite) to confirm no regressions.

    **TypeScript compilation check:**
    11. Run `npx tsc --noEmit` to verify the new files compile without errors under strict mode.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/gdpr.test.ts</automated>
  </verify>
  <done>
    - All unit tests pass (blocked paths return true, allowed paths return false, normalization works, null/undefined handled)
    - `npx tsc --noEmit` succeeds (no TypeScript errors)
    - `npx vitest run` full suite passes (no regressions)
    - `src/gdpr.ts` exports only `isBlocked` (no normalizePath export)
    - "clients" literal is hardcoded inside the function body
  </done>
</task>

</tasks>

<verification>
1. `npx vitest run src/__tests__/gdpr.test.ts` -- all GDPR predicate tests green
2. `npx vitest run` -- full test suite green (no regressions)
3. `npx tsc --noEmit` -- TypeScript compiles cleanly
4. `grep -c "export" src/gdpr.ts` returns 1 (only isBlocked exported)
</verification>

<success_criteria>
- isBlocked("Clients/AcmeCorp") returns true
- isBlocked("Clients") returns false
- isBlocked("Clients/Acme/SubPage") returns false
- All normalization variants (slashes, case) produce correct results
- Null/undefined/empty inputs return false without throwing
- Full test suite passes with zero regressions
- TypeScript strict mode compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/22-core-gdpr-predicate/22-01-SUMMARY.md`
</output>
