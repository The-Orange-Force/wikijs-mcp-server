---
phase: 18-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types.ts
  - src/api.ts
  - src/mcp-tools.ts
  - src/scopes.ts
  - src/routes/public-routes.ts
  - package.json
autonomous: true
requirements: [CLEN-03]

must_haves:
  truths:
    - "WikiJsUser, WikiJsGroup, ResponseResult types do not exist in src/types.ts"
    - "Dead API methods (createPage, updatePage, deletePage, etc.) do not exist in src/api.ts"
    - "api.ts imports only WikiJsPage from types.ts"
    - "Version strings read 2.3.0 in package.json, mcp-tools.ts, and public-routes.ts"
    - "npm run build compiles with zero errors"
  artifacts:
    - path: "src/types.ts"
      provides: "Only WikiJsPage type"
      contains: "WikiJsPage"
    - path: "src/api.ts"
      provides: "5 methods: checkConnection, getPageById, getPageContent, getPagesList, searchPages"
    - path: "src/scopes.ts"
      provides: "Single scope wikijs:read mapping to surviving tools"
      contains: "wikijs:read"
    - path: "package.json"
      provides: "Version 2.3.0"
      contains: "2.3.0"
  key_links:
    - from: "src/api.ts"
      to: "src/types.ts"
      via: "import"
      pattern: "import.*WikiJsPage.*from.*types"
    - from: "src/mcp-tools.ts"
      to: "src/api.ts"
      via: "method calls"
      pattern: "wikiJsApi\\.(getPageById|getPageContent|getPagesList|searchPages)"
---

<objective>
Remove dead source code left after Phases 15-17 and synchronize version strings to 2.3.0.

Purpose: Phases 15-17 consolidate tools and scopes. This plan removes any remaining dead types, API methods, tool registrations, and scope entries, then bumps all version strings. The build must compile cleanly after these changes.

Output: Clean src/ directory with only surviving code, consistent 2.3.0 version across all three locations.
</objective>

<execution_context>
@/Users/magnetrong/.claude/get-shit-done/workflows/execute-plan.md
@/Users/magnetrong/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-cleanup/18-RESEARCH.md

<!-- Phase 15-17 SUMMARYs should be referenced here once they exist -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead types, API methods, and scope entries</name>
  <files>src/types.ts, src/api.ts, src/mcp-tools.ts, src/scopes.ts</files>
  <action>
This task removes dead code remaining after Phases 15-17. Before editing, grep each file to confirm what is still present (Phases 15-17 may have already removed some items).

**src/types.ts** -- Remove these types if they still exist:
- `WikiJsToolDefinition` interface (dead -- not imported anywhere in src/)
- `WikiJsUser` interface (dead after user tools removed)
- `WikiJsGroup` interface (dead after group tools removed)
- `ResponseResult` interface (dead after mutation methods removed)
Keep only `WikiJsPage`. Remove Russian comments if present; add English module comment.

**src/api.ts** -- Remove these methods if they still exist:
- `createPage`, `updatePage`, `deletePage`, `forceDeletePage`, `publishPage`
- `getUsersList`, `searchUsers`, `getGroupsList`, `createUser`, `updateUser`
- `getAllPagesList`, `searchUnpublishedPages`, `getPageStatus`
Update the import statement at top to import only `WikiJsPage` (remove `WikiJsUser`, `WikiJsGroup`, `ResponseResult` imports).
After cleanup, api.ts should have exactly 5 methods: `checkConnection`, `getPageById`, `getPageContent`, `getPagesList`, `searchPages`.

**src/mcp-tools.ts** -- Remove dead tool registrations if they still exist:
- `create_page`, `update_page`, `delete_page`, `list_all_pages`, `search_unpublished_pages`
- `force_delete_page`, `get_page_status`, `publish_page`
- `list_users`, `search_users`, `create_user`, `list_groups`, `update_user`
Remove dead tool name constants. Update module docstring to reflect surviving tool count (3 or 4 depending on whether get_page_content was merged in Phase 15).

**src/scopes.ts** -- Simplify if not already done by Phase 17:
- Remove `WRITE` and `ADMIN` from `SCOPES` constant
- Remove their entries from `SCOPE_TOOL_MAP`
- Update `wikijs:read` entry to contain only surviving tools
- Keep the `SUPPORTED_SCOPES` and `TOOL_SCOPE_MAP` derived constants (they auto-update)

Run `npx tsc --noEmit` after each file change to catch compilation errors early. Work in consumer-first order: mcp-tools.ts first, then api.ts imports, then api.ts methods, then types.ts.
  </action>
  <verify>
    <automated>cd /Users/magnetrong/git/wikijs-mcp-server && npx tsc --noEmit && echo "BUILD OK"</automated>
  </verify>
  <done>
- WikiJsUser, WikiJsGroup, ResponseResult, WikiJsToolDefinition do not exist in types.ts
- api.ts has exactly 5 methods and imports only WikiJsPage
- mcp-tools.ts registers only surviving tools (3 or 4)
- scopes.ts has only wikijs:read scope
- TypeScript compiles with zero errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Synchronize version strings to 2.3.0</name>
  <files>package.json, src/mcp-tools.ts, src/routes/public-routes.ts</files>
  <action>
Update version to "2.3.0" in all three locations:

1. **package.json** line 3: `"version": "1.3.0"` -> `"2.3.0"`
2. **package.json** line 4: Update `"description"` to reflect read-only MCP server (e.g., "MCP Server providing read-only Wiki.js page access via GraphQL")
3. **src/mcp-tools.ts** line 24: `version: "1.3.0"` -> `version: "2.3.0"` in McpServer config
4. **src/routes/public-routes.ts** line 39: `version: "2.0.0"` -> `version: "2.3.0"` in GET / response

Run `npm run build` to verify full compilation succeeds.
  </action>
  <verify>
    <automated>cd /Users/magnetrong/git/wikijs-mcp-server && npm run build && grep '"2.3.0"' package.json src/mcp-tools.ts src/routes/public-routes.ts | wc -l | xargs test 3 -eq && echo "VERSION SYNC OK"</automated>
  </verify>
  <done>
- All three files contain "2.3.0"
- package.json description reflects read-only server
- npm run build completes cleanly
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes (no type errors)
- `npm run build` succeeds (dist/ output clean)
- `grep -r "WikiJsUser\|WikiJsGroup\|ResponseResult\|WikiJsToolDefinition" src/` returns no matches
- `grep -c '"2.3.0"' package.json src/mcp-tools.ts src/routes/public-routes.ts` shows 1 match in each file
</verification>

<success_criteria>
All dead types, API methods, tool registrations, and scope entries removed from src/. Version 2.3.0 consistent across package.json, mcp-tools.ts, and public-routes.ts. TypeScript compiles cleanly.
</success_criteria>

<output>
After completion, create `.planning/phases/18-cleanup/18-01-SUMMARY.md`
</output>
