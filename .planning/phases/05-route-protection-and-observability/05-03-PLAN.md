---
phase: 05-route-protection-and-observability
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/auth/middleware.ts
  - src/routes/mcp-routes.ts
  - tests/route-protection.test.ts
autonomous: true
gap_closure: true
requirements:
  - PROT-01
  - PROT-02
  - PROT-03
  - PROT-04
  - OBSV-01
  - OBSV-02
  - OBSV-03

must_haves:
  truths:
    - "Auth error response JSON bodies include correlation_id field matching X-Request-ID header"
    - "No dead imports exist in src/routes/mcp-routes.ts"
  artifacts:
    - path: "src/auth/middleware.ts"
      provides: "correlation_id in every error response body"
      contains: "correlation_id: request.id"
    - path: "src/routes/mcp-routes.ts"
      provides: "Clean imports with no dead references"
    - path: "tests/route-protection.test.ts"
      provides: "Assertions that body.correlation_id matches x-request-id header"
  key_links:
    - from: "src/auth/middleware.ts"
      to: "request.id"
      via: "correlation_id field in .send() JSON body"
      pattern: "correlation_id.*request\\.id"
---

<objective>
Close two verification gaps from Phase 5 VERIFICATION.md:

1. Add correlation_id to auth middleware error response bodies (OBSV-02 partial)
2. Remove orphaned auth-errors.ts imports from mcp-routes.ts (OBSV-03 wiring)

Purpose: Satisfy ROADMAP success criterion 4 ("correlation ID appears in error response bodies") and remove misleading dead code.
Output: Updated middleware, cleaned imports, new test assertions.
</objective>

<execution_context>
@/Users/magnetrong/.claude/get-shit-done/workflows/execute-plan.md
@/Users/magnetrong/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-route-protection-and-observability/05-VERIFICATION.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/auth/middleware.ts:
- Lines 62, 71, 108, 130, 140: `.send({ error: ..., error_description: ... })` calls that need `correlation_id: request.id` added
- Line 94-98: `.send({ error: ..., error_description: ..., required_scopes: ... })` needs `correlation_id: request.id` added
- `request.id` is a string set by Fastify's genReqId (UUID format, validated in src/logging.ts)

From src/routes/mcp-routes.ts:
- Lines 17-20: Dead imports to remove:
  ```typescript
  import {
    mapJoseErrorToRfc6750,
    mapMissingTokenError,
  } from "../auth-errors.js";
  ```
- These functions are never called anywhere in the file.

From tests/route-protection.test.ts:
- Line 82-105: "POST /mcp without token returns 401" test -- add `body.correlation_id` assertion
- Line 107-118: "POST /mcp with 'Bearer invalid' returns 401" test -- add `body.correlation_id` assertion
- Line 120-128: "GET /mcp without token returns 401" test -- add `body.correlation_id` assertion
- Line 202-212: "401 error response includes X-Request-ID header" test -- add body correlation_id check
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add correlation_id to auth middleware error responses and test assertions</name>
  <files>src/auth/middleware.ts, tests/route-protection.test.ts</files>
  <action>
In src/auth/middleware.ts, add `correlation_id: request.id` to every `.send()` call that returns an error response body. There are 6 send calls:

1. Line 62: `.send({ error: 'invalid_token', error_description: 'missing bearer token' })`
   Change to: `.send({ error: 'invalid_token', error_description: 'missing bearer token', correlation_id: request.id })`

2. Line 71: `.send({ error: 'invalid_token', error_description: 'missing bearer token' })`
   Change to: `.send({ error: 'invalid_token', error_description: 'missing bearer token', correlation_id: request.id })`

3. Lines 94-98: `.send({ error: 'insufficient_scope', error_description: '...', required_scopes: [...] })`
   Add `correlation_id: request.id` to the object.

4. Line 108: `.send({ error: 'invalid_token', error_description: 'missing required claim: oid' })`
   Change to: `.send({ error: 'invalid_token', error_description: 'missing required claim: oid', correlation_id: request.id })`

5. Line 130: `.send({ error: mapped.error, error_description: mapped.description })`
   Change to: `.send({ error: mapped.error, error_description: mapped.description, correlation_id: request.id })`

6. Line 140: `.send({ error: mapped.error, error_description: mapped.description })`
   Change to: `.send({ error: mapped.error, error_description: mapped.description, correlation_id: request.id })`

In tests/route-protection.test.ts, add correlation_id body assertions to existing auth error tests:

- In "POST /mcp without token returns 401" test (line 82): after `expect(body.error_description).toBeDefined()`, add:
  ```typescript
  expect(body.correlation_id).toBeDefined();
  expect(body.correlation_id).toBe(res.headers["x-request-id"]);
  ```

- In "POST /mcp with 'Bearer invalid' returns 401" test (line 107): parse body as JSON and assert:
  ```typescript
  const body = res.json();
  expect(body.correlation_id).toBe(res.headers["x-request-id"]);
  ```

- In "GET /mcp without token returns 401" test (line 120): parse body as JSON and assert:
  ```typescript
  const body = res.json();
  expect(body.correlation_id).toBe(res.headers["x-request-id"]);
  ```

- In "401 error response includes X-Request-ID header" test (line 202): parse body and add:
  ```typescript
  const body = res.json();
  expect(body.correlation_id).toBeDefined();
  expect(body.correlation_id).toBe(res.headers["x-request-id"]);
  ```
  </action>
  <verify>
    <automated>cd /Users/magnetrong/git/wikijs-mcp-server && npx vitest run tests/route-protection.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>All 6 .send() calls in src/auth/middleware.ts include correlation_id: request.id. All route-protection tests pass with body.correlation_id assertions matching X-Request-ID header.</done>
</task>

<task type="auto">
  <name>Task 2: Remove orphaned auth-errors.ts imports from mcp-routes.ts</name>
  <files>src/routes/mcp-routes.ts</files>
  <action>
Remove the unused import block at lines 17-20 of src/routes/mcp-routes.ts:

```typescript
import {
  mapJoseErrorToRfc6750,
  mapMissingTokenError,
} from "../auth-errors.js";
```

Delete these 4 lines entirely. No replacement needed -- Phase 4's auth plugin (src/auth/errors.ts) handles all production auth error mapping already.

Also update the file's module-level JSDoc comment (lines 1-11) to remove the reference to "Phase 5 Plan 01's RFC 6750 error mapping for structured error responses" since that module is no longer imported. Replace with a note that auth errors are handled by the scoped auth plugin.

Verify TypeScript compiles cleanly after the removal:
```bash
npx tsc --noEmit
```
  </action>
  <verify>
    <automated>cd /Users/magnetrong/git/wikijs-mcp-server && npx tsc --noEmit 2>&1 && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <done>mcp-routes.ts has no imports from auth-errors.ts. TypeScript compiles with zero errors. All tests pass.</done>
</task>

</tasks>

<verification>
1. `npx vitest run tests/route-protection.test.ts` -- all tests pass including new correlation_id assertions
2. `npx tsc --noEmit` -- zero TypeScript errors
3. `npx vitest run` -- full test suite passes (no regressions)
4. `grep -n 'correlation_id' src/auth/middleware.ts` -- shows correlation_id in all 6 send calls
5. `grep 'auth-errors' src/routes/mcp-routes.ts` -- returns no matches (dead imports removed)
</verification>

<success_criteria>
- Auth error response bodies contain correlation_id matching X-Request-ID header (OBSV-02 fully satisfied)
- No dead imports from auth-errors.ts in mcp-routes.ts (OBSV-03 wiring gap closed)
- Full test suite passes with zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/05-route-protection-and-observability/05-03-SUMMARY.md`
</output>
