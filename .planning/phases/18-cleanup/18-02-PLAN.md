---
phase: 18-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mcp_wikijs_stdin.js
  - lib/README.md
  - scripts/start_http.sh
  - scripts/stop_server.sh
  - scripts/setup.sh
  - scripts/start_typescript.sh
  - Dockerfile
  - .dockerignore
autonomous: true
requirements: [CLEN-01, CLEN-02]

must_haves:
  truths:
    - "lib/ directory does not exist"
    - "No file in the project references mcp_wikijs_stdin or STDIO transport"
    - "@azure/msal-node does not appear in package.json or package-lock.json"
    - "Dockerfile uses node:20-alpine base image"
    - "Dockerfile does not COPY lib/"
    - "Shell scripts contain no STDIO references"
    - "package.json has no server:stdio or start:typescript scripts"
    - "scripts/start_typescript.sh does not exist"
  artifacts:
    - path: "Dockerfile"
      provides: "Alpine-based multi-stage Docker build"
      contains: "node:20-alpine"
    - path: "scripts/start_http.sh"
      provides: "HTTP server start without STDIO references"
    - path: "scripts/stop_server.sh"
      provides: "Server stop without STDIO process kill"
    - path: "scripts/setup.sh"
      provides: "Setup script without STDIO instructions"
  key_links:
    - from: "Dockerfile"
      to: "package.json"
      via: "npm ci reads dependencies"
      pattern: "npm ci"
    - from: "package.json"
      to: "dist/server.js"
      via: "scripts.start"
      pattern: "start_http.sh"
---

<objective>
Remove STDIO transport files, uninstall @azure/msal-node, clean shell scripts, and switch Docker to Alpine.

Purpose: CLEN-01 requires complete STDIO transport removal. CLEN-02 requires msal-node removal. The user decided to switch to Alpine (now possible since the msal-node native module blocker is gone) and clean all script references.

Output: No lib/ directory, no msal-node dependency, clean scripts, Alpine-based Dockerfile.
</objective>

<execution_context>
@/Users/magnetrong/.claude/get-shit-done/workflows/execute-plan.md
@/Users/magnetrong/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-cleanup/18-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove STDIO transport and clean scripts</name>
  <files>lib/mcp_wikijs_stdin.js, lib/README.md, scripts/start_http.sh, scripts/stop_server.sh, scripts/setup.sh, scripts/start_typescript.sh, package.json</files>
  <action>
**Delete files:**
- `rm -rf lib/` (removes mcp_wikijs_stdin.js and README.md)
- `rm scripts/start_typescript.sh` (dead code -- functionally identical to start_http.sh, per CONTEXT.md Claude's discretion)

**Edit package.json** (use jq or manual edit):
- Remove `"server:stdio": "node lib/mcp_wikijs_stdin.js"` from scripts
- Remove `"start:typescript": "./scripts/start_typescript.sh"` from scripts
- Remove `"lib/"` from the `files` array

**Edit scripts/start_http.sh:**
- Remove line 15: `pkill -f "mcp_wikijs_stdin.js" || true`

**Edit scripts/stop_server.sh:**
- Remove line 35: `pkill -f "mcp_wikijs_stdin.js" && echo "Stopped mcp_wikijs_stdin.js" || echo "mcp_wikijs_stdin.js not running"`
- Line 41: Change grep pattern from `"(dist/server\.js|mcp_wikijs_stdin)"` to `"dist/server\.js"` (remove mcp_wikijs_stdin from the pattern)
- Line 47: Same change -- remove `|mcp_wikijs_stdin` from the grep pattern

**Edit scripts/setup.sh:**
- Remove line 48: `echo -e "${YELLOW}To start the STDIO server:${NC} npm run server:stdio"`

After edits, verify: `grep -r "mcp_wikijs_stdin\|server:stdio\|start:typescript\|STDIO" scripts/ package.json lib/ 2>/dev/null` should return nothing.
  </action>
  <verify>
    <automated>cd /Users/magnetrong/git/wikijs-mcp-server && test ! -d lib && test ! -f scripts/start_typescript.sh && ! grep -r "mcp_wikijs_stdin\|server:stdio\|start:typescript" scripts/ package.json 2>/dev/null && echo "STDIO REMOVED OK"</automated>
  </verify>
  <done>
- lib/ directory does not exist
- scripts/start_typescript.sh does not exist
- No STDIO references in scripts/ or package.json
- server:stdio and start:typescript npm scripts removed
- lib/ removed from package.json files array
  </done>
</task>

<task type="auto">
  <name>Task 2: Uninstall msal-node and switch Dockerfile to Alpine</name>
  <files>package.json, Dockerfile, .dockerignore</files>
  <action>
**Uninstall @azure/msal-node:**
Run `npm uninstall @azure/msal-node` -- this updates both package.json and package-lock.json.
Verify with `grep "msal-node" package.json` (should return nothing).

**Update Dockerfile:**
1. Line 4-5 (comment): Remove or update the comment about msal-node/Alpine compatibility. Replace with: `# node:20-alpine is used for minimal image size. All remaining dependencies are pure JavaScript.`
2. Line 7: `FROM node:20-slim AS builder` -> `FROM node:20-alpine AS builder`
3. Line 35: `FROM node:20-slim AS runtime` -> `FROM node:20-alpine AS runtime`
4. Lines 49-50: Delete `# Copy STDIO transport stub` comment and `COPY lib/ ./lib/` line
5. Line 53 (USER comment): Update `node:20-slim` reference to `node:20-alpine` in the comment about the `node` user
6. Lines 58-59 (HEALTHCHECK comment): Update `node:20-slim` reference to `node:20-alpine` in the comment about curl/wget

**Check .dockerignore:**
Per research, no changes needed (.dockerignore has no lib/ exceptions). Verify with `grep "lib" .dockerignore` and confirm no changes required.

**Verify Docker build:**
Run `docker build -t wikijs-mcp:cleanup-test .` to confirm Alpine image builds successfully. If docker is not available, this is acceptable -- the build will be verified during phase gate.
  </action>
  <verify>
    <automated>cd /Users/magnetrong/git/wikijs-mcp-server && ! grep "msal-node" package.json && grep "node:20-alpine" Dockerfile | wc -l | xargs test 2 -eq && ! grep "COPY lib/" Dockerfile && echo "INFRA CLEANUP OK"</automated>
  </verify>
  <done>
- @azure/msal-node not in package.json or package-lock.json
- Dockerfile uses node:20-alpine for both builder and runtime stages
- Dockerfile does not COPY lib/
- Dockerfile comments updated to reference Alpine instead of slim
- Docker build succeeds (or deferred to phase gate if docker unavailable)
  </done>
</task>

</tasks>

<verification>
- `test ! -d lib` (lib/ directory gone)
- `! grep -r "mcp_wikijs_stdin" scripts/ package.json Dockerfile` (no STDIO references)
- `! grep "msal-node" package.json` (msal-node removed)
- `grep -c "node:20-alpine" Dockerfile` returns 2 (builder + runtime)
- `! grep "COPY lib/" Dockerfile` (no lib COPY)
- `docker build -t wikijs-mcp:cleanup-test .` succeeds (if docker available)
</verification>

<success_criteria>
STDIO transport completely removed from all files. @azure/msal-node uninstalled. Dockerfile uses Alpine base. All shell scripts free of STDIO references. Docker image builds successfully on Alpine.
</success_criteria>

<output>
After completion, create `.planning/phases/18-cleanup/18-02-SUMMARY.md`
</output>
