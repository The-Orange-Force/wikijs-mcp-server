---
phase: 19-instructions-loading-and-initialize-response
plan: 02
type: execute
wave: 2
depends_on:
  - "19-01"
files_modified:
  - src/server.ts
  - src/mcp-tools.ts
  - src/routes/mcp-routes.ts
  - src/routes/public-routes.ts
  - tests/helpers/build-test-app.ts
  - tests/smoke.test.ts
  - package.json
autonomous: true
requirements:
  - INIT-01
  - INIT-02

must_haves:
  truths:
    - "MCP initialize response includes an instructions field with text content"
    - "Instructions text mentions Mendix, client names, AI, Java, and career topics"
    - "Server version is 2.4.0 in all locations"
    - "createMcpServer receives instructions string and passes to McpServer constructor"
    - "Instructions are loaded once at startup and threaded through to per-request McpServer creation"
  artifacts:
    - path: "src/mcp-tools.ts"
      provides: "createMcpServer with instructions parameter"
      exports: ["createMcpServer"]
    - path: "src/server.ts"
      provides: "Startup calls loadInstructions and threads result through buildApp"
    - path: "src/routes/mcp-routes.ts"
      provides: "ProtectedRoutesOptions with instructions field"
  key_links:
    - from: "src/server.ts"
      to: "src/instructions.ts"
      via: "await loadInstructions(config.instructionsPath) in start()"
      pattern: "loadInstructions"
    - from: "src/server.ts"
      to: "src/routes/mcp-routes.ts"
      via: "passes instructions in protectedRoutes opts"
      pattern: "instructions"
    - from: "src/routes/mcp-routes.ts"
      to: "src/mcp-tools.ts"
      via: "createMcpServer(wikiJsApi, instructions)"
      pattern: "createMcpServer.*instructions"
    - from: "src/mcp-tools.ts"
      to: "@modelcontextprotocol/sdk"
      via: "new McpServer(serverInfo, { instructions })"
      pattern: "instructions"
---

<objective>
Wire the instructions string from startup through to the MCP initialize response and bump version to 2.4.0.

Purpose: Make the loaded instructions appear in the MCP protocol's initialize response so that Claude Desktop and other MCP clients automatically receive behavioral guidance. This completes the Phase 19 feature.

Output: Updated server.ts (startup loading), mcp-routes.ts (threading), mcp-tools.ts (McpServer constructor), public-routes.ts (version bump), integration tests verifying the initialize response.
</objective>

<execution_context>
@/Users/magnetrong/.claude/get-shit-done/workflows/execute-plan.md
@/Users/magnetrong/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-instructions-loading-and-initialize-response/19-CONTEXT.md
@.planning/phases/19-instructions-loading-and-initialize-response/19-01-SUMMARY.md

@src/server.ts
@src/mcp-tools.ts
@src/routes/mcp-routes.ts
@src/routes/public-routes.ts
@tests/helpers/build-test-app.ts
@tests/smoke.test.ts
</context>

<interfaces>
<!-- Key types and contracts from Plan 01 output that this plan consumes. -->

From src/instructions.ts (created by Plan 01):
```typescript
export const DEFAULT_INSTRUCTIONS: string;
export async function loadInstructions(path?: string): Promise<string>;
```

From src/config.ts (updated by Plan 01):
```typescript
export type AppConfig = {
  port: number;
  wikijs: { baseUrl: string; token: string };
  azure: { tenantId: string; clientId: string; resourceUrl: string; resourceDocsUrl?: string; jwksUri: string; issuer: string };
  instructionsPath?: string;  // NEW from Plan 01
};
```

From src/routes/mcp-routes.ts (current):
```typescript
export interface ProtectedRoutesOptions {
  wikiJsApi: WikiJsApi;
  auth: AuthPluginOptions;
}
```

From src/mcp-tools.ts (current):
```typescript
export function createMcpServer(wikiJsApi: WikiJsApi): McpServer;
```

From @modelcontextprotocol/sdk:
```typescript
// McpServer constructor:
constructor(serverInfo: Implementation, options?: ServerOptions);
// ServerOptions includes:
instructions?: string;
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Wire instructions through server, routes, and MCP tools + version bump</name>
  <files>src/server.ts, src/mcp-tools.ts, src/routes/mcp-routes.ts, src/routes/public-routes.ts, package.json</files>
  <action>
    Thread the instructions string from startup through to the McpServer constructor, and bump version to 2.4.0.

    **1. src/mcp-tools.ts** -- Add instructions parameter:
    - Change function signature: `export function createMcpServer(wikiJsApi: WikiJsApi, instructions: string): McpServer`
    - Change McpServer constructor: `new McpServer({ name: "wikijs-mcp", version: "2.4.0" }, { instructions })`
    - Version bump from "2.3.0" to "2.4.0"

    **2. src/routes/mcp-routes.ts** -- Add instructions to options and handler:
    - Add `instructions: string` to `ProtectedRoutesOptions` interface
    - Destructure: `const { wikiJsApi, auth, instructions } = opts;`
    - Update POST /mcp handler: `const mcpServer = createMcpServer(wikiJsApi, instructions);`

    **3. src/server.ts** -- Load instructions at startup and pass through:
    - Add import: `import { loadInstructions } from "./instructions.js";`
    - `buildApp()` signature change: `export function buildApp(appConfig: AppConfig, wikiJsApiOverride?: WikiJsApi, instructions?: string): FastifyInstance`
       - The instructions parameter defaults to DEFAULT_INSTRUCTIONS if not provided (for test compatibility): add `import { DEFAULT_INSTRUCTIONS } from "./instructions.js";` and use `const effectiveInstructions = instructions ?? DEFAULT_INSTRUCTIONS;`
    - In the `server.register(protectedRoutes, {...})` call, add `instructions: effectiveInstructions` to the options object
    - In `start()`: After `logConfig(config)`, add `const instructions = await loadInstructions(config.instructionsPath);` then pass to `buildApp(config, undefined, instructions)` -- note buildApp now takes 3 args

    **4. src/routes/public-routes.ts** -- Version bump only:
    - Change `version: "2.3.0"` to `version: "2.4.0"` in the GET / response

    **5. package.json** -- Version bump:
    - Change `"version": "2.3.0"` to `"version": "2.4.0"`

    IMPORTANT: All new imports must use `.js` extension (e.g., `./instructions.js`).
  </action>
  <verify>
    <automated>npx vitest run tests/config.test.ts tests/instructions.test.ts</automated>
  </verify>
  <done>
    - createMcpServer accepts (wikiJsApi, instructions) and passes instructions to McpServer constructor
    - ProtectedRoutesOptions includes instructions field
    - buildApp threads instructions to protectedRoutes
    - start() loads instructions via loadInstructions(config.instructionsPath) before buildApp
    - Version is 2.4.0 in mcp-tools.ts, public-routes.ts, and package.json
  </done>
</task>

<task type="auto">
  <name>Task 2: Update test helpers and integration tests for instructions in initialize response</name>
  <files>tests/helpers/build-test-app.ts, tests/smoke.test.ts</files>
  <action>
    Update test infrastructure and add assertions for instructions in the MCP initialize response.

    **1. tests/helpers/build-test-app.ts** -- Thread instructions into test app:
    - Add import: `import { DEFAULT_INSTRUCTIONS } from "../../src/instructions.js";`
    - In `buildTestApp()`, add `instructions` to the `protectedRoutes` registration:
      ```
      server.register(protectedRoutes, {
        wikiJsApi,
        instructions: DEFAULT_INSTRUCTIONS,
        auth: { ... },
      });
      ```
    - Optionally add an `instructions?: string` parameter to `buildTestApp()` for tests that want custom instructions, defaulting to `DEFAULT_INSTRUCTIONS`.

    **2. tests/smoke.test.ts** -- Add/update integration tests:

    a) Update the existing "POST /mcp with initialize request returns a valid JSON-RPC response" test:
    - After the existing assertions, add:
      ```
      expect(data.result.instructions).toBeDefined();
      expect(typeof data.result.instructions).toBe("string");
      expect(data.result.instructions.length).toBeGreaterThan(0);
      ```

    b) Add a new test case in a new describe block "INIT-01/INIT-02: Instructions in initialize response":
    - Test: "initialize response includes instructions with all 5 topic areas"
      - Send initialize JSON-RPC request
      - Parse response
      - Assert `data.result.instructions` contains "Mendix" (case-insensitive)
      - Assert `data.result.instructions` contains "client" (case-insensitive)
      - Assert `data.result.instructions` contains "AI" (case-sensitive, it's an acronym)
      - Assert `data.result.instructions` contains "Java" (case-sensitive)
      - Assert `data.result.instructions` contains "career" (case-insensitive)
      - Assert `data.result.instructions` does NOT contain "search_pages" or "get_page" or "list_pages" (no tool names)

    c) Update the existing version assertion:
    - Change `expect(data.version).toBe("2.3.0")` to `expect(data.version).toBe("2.4.0")` wherever it appears in smoke.test.ts

    d) Update the module import test to pass an instructions argument:
    - Change `createMcpServer(mockApi)` to `createMcpServer(mockApi, "test instructions")`
  </action>
  <verify>
    <automated>npx vitest run tests/smoke.test.ts</automated>
  </verify>
  <done>
    - buildTestApp threads DEFAULT_INSTRUCTIONS to protectedRoutes
    - Initialize response contains instructions field with string content
    - Instructions mention all 5 topics: Mendix, client names, AI, Java, career
    - Instructions do NOT contain tool names
    - Version assertions updated to 2.4.0
    - Full test suite passes: `npx vitest run`
  </done>
</task>

</tasks>

<verification>
- `npx vitest run tests/smoke.test.ts` -- initialize response includes instructions with 5 topics, version 2.4.0
- `npx vitest run` -- full test suite green (no regressions)
- `npm run build` -- TypeScript compiles without errors
</verification>

<success_criteria>
- MCP initialize response includes `instructions` field with meaningful text content
- Instructions text mentions Mendix, client names, AI, Java, and career
- Instructions do NOT mention specific tool names
- Version is 2.4.0 across all relevant files (mcp-tools.ts, public-routes.ts, package.json)
- All existing tests pass with updated version expectations
- TypeScript builds successfully
</success_criteria>

<output>
After completion, create `.planning/phases/19-instructions-loading-and-initialize-response/19-02-SUMMARY.md`
</output>
